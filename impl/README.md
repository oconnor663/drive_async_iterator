This is the internal proc macro crate for the `drive_async_iterator::drive!`
macro. Rust requires that proc macros and their support library code to be
separated into different crates. Callers should depend on
`drive_async_iterator` instead.
